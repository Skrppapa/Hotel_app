# API для бронирования отелей (Hotel Booking API)

REST API сервис для поиска и бронирования номеров в отелях. Проект разрабатывается в рамках курса «Backend разработка на Python» (PyTex) с упором на чистую архитектуру и современные стандарты веб-разработки.

## 🛠 Технологии и стек

**Язык и фреймворки:**
* Python 3.10+
* FastAPI
* Pydantic (валидация данных)

**Базы данных и ORM:**
* PostgreSQL (основная реляционная БД)
* SQLAlchemy (взаимодействие с БД)
* Alembic (управление миграциями)
* Redis (кэширование данных)

**Безопасность:**
* JWT (аутентификация и авторизация)

## ⚙️ Реализованный функционал (Бизнес-логика)
* **Пользователи:** Регистрация, аутентификация по JWT-токену, управление профилем.
* **Отели и номера:** Получение списка доступных отелей, фильтрация и просмотр свободных номеров на выбранные даты.
* **Бронирование:** Создание бронирования, проверка доступности номера, защита от двойного бронирования (Race Condition).
* **Производительность:** Кэширование частых запросов с использованием Redis для ускорения времени ответа API.

## 🚀 Как запустить проект локально

1. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/Skrppapa/Hotel_app.git
   cd Hotel_app

    Создайте и активируйте виртуальное окружение:
    Установите зависимости:
    Настройте переменные окружения: Создайте файл .env в корне проекта (пример можно посмотреть в .env.example) и укажите доступы к вашей БД PostgreSQL и Redis:
    Примените миграции базы данных:
    Запустите сервер:
    После запуска API будет доступно по адресу: http://localhost:8000 Интерактивная документация (Swagger): http://localhost:8000/docs

🗺 Roadmap (В процессе внедрения)
В данный момент проект дорабатывается. В ближайших планах:

    [ ] Docker & Docker Compose: Контейнеризация приложения и баз данных для запуска одной командой.
    [ ] Celery: Настройка фоновых задач (например, отправка email-уведомлений о бронировании).
    [ ] Pytest: Покрытие бизнес-логики и эндпоинтов unit- и интеграционными тестами.